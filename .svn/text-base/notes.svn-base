проблема:

внутри Template.body нужно иметь доступ к i через некий интерфейс,
учитывающий понятие, соответствующее данному шаблону

т. е. body должен зависить от функции, дающей доступ к свойствам переданного ресурса

даже лучше внутри body использовать набор геттеров для свойств ресурса

такой набор геттеров определяется онтологией

при выполнении шаблонов геттеры применяются к переданному ресурсу

при проверке --- просто проверяется, есть ли нужное свойство у класса

получается, что body имеет тип PropertySet -> A, где PropertySet --- набор геттеров для свойств,
работающих по разному при выполнении и при проверке шаблона

при этом PropertySet должен задаваться для всей системы шаблонов в целом



проблема:

Есть классы: Individual < PropertySet

Individual хранит в себе данные --- набор пар "имя-значение"
PropertySet содержит определения функций для доступа к отдельным свойствам 

